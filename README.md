# Simple Window Manager

Super simple window management for windows. Emphasis on the
simple, de-emphasis on the management.

Run a Docker-based devenv with all dependencies you need with
`./devenv/run`. Once inside, use scripts in the `./tools`
directory to interact with the project
